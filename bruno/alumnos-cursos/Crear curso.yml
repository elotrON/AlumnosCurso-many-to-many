info:
  name: Crear curso
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/curso"
  body:
    type: json
    data: |-
      {
        "nombre": "Agricultura moderna",
        "duracion": "37",
        "habilidades":"cultivo tomates"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
