info:
  name: agregar curso a alumno
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/alumno/2
  body:
    type: json
    data: |-
      {
        "id_curso":"2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
