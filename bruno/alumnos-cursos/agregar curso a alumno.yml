info:
  name: agregar curso a alumno
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/alumno/5"
  body:
    type: json
    data: |-
      {
        "idCurso":"1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
